# import { gql } from "@apollo/client";

# export const GET_Products = gql`
  query getProducts {
    products {
      results {
        id
        masterData {
          current {
            name(locale: "en")
          }
        }
      }
    }
  }
# `;
